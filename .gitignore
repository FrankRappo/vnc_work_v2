# scratch capture output — real screens/crops land here at runtime
screens/*.png
screens/*.jpg
screens/.offset
__pycache__/
*.pyc
# fixture-generated artifacts (regenerated by make_fixtures.py / tests)
fixtures/_crop_test.png
fixtures/.ctcnt
fixtures/*.ocr*
# кадры, убранные командой `clean` (attic = карантин залежавшихся снимков, не история проекта)
screens/attic/
screens_t67/attic/
srvshot/attic/

# ── T201: секреты каналов и GUI-клиентов. В git не должны попасть НИКОГДА ──
# приватные ключи обратных SSH-туннелей + known_hosts + креды Windows
ssh_channel/
# снимок настроек RustDesk/AnyDesk: пароли, токены, ключ нашего rendezvous-сервера
profiles/
*.pem
id_rsa*
id_ed25519*
*_me
*_tunnel
*.creds
# кадры боевых экранов магазина (любой каталог захвата целиком)
screens/
screens_t67/
srvshot/
# профили браузера с ЖИВОЙ залогиненной сессией — данные, не код
profile_backups/*/

# Временные кропы и кадры задач (12.09.2026): мусор по 0,4 МБ, в git им не место —
# кадры живут в evidence/ и screens/, а эти оседали в корне после прогонов T317/T318/T320.
t[0-9][0-9][0-9]_*.png
*_crop*.png
*_grid.png
