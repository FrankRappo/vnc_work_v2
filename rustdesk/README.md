# RustDesk-канал к кассе КСО (через СВОЙ сервер)

Настроено 2026-06-28. Заменяет AnyDesk-канал (free: нет буфера под большой код + 5-мин обрывы)
и публичный сервер RustDesk (теперь блокирует коннекты без логина — анти-абьюз).

## Зачем свой сервер
Публичный сервер RustDesk (`rs-*.rustdesk.com`) с 2026 требует вход (Google/GitHub) перед
коннектом и прямо пишет «не для продакшна». Поэтому подняли собственный hbbs+hbbr на VPS —
без логин-стены, с рабочим буфером обмена и без лимитов по времени.

## Сервер (VPS Aeza)
- **Адрес (ID Server / Relay Server): `178.253.55.128`**
- **Key (публичный ключ hbbs): `OS6FFMq66QzmnraQDy+cy+fdryWDXA0fBVZTbYqc7Lk=`**
- API Server: пусто.
- Доступ к VPS: см. `/home/hgff/vps for cron.txt` (root@178.253.55.128:22).
- Службы: `rustdesk-hbbs.service`, `rustdesk-hbbr.service` (systemd, enabled, Restart=always).
- Бинарники: `/usr/local/bin/hbbs`, `/usr/local/bin/hbbr`. Данные/ключ: `/var/lib/rustdesk-server/`.
- Порты: hbbs 21115/tcp, 21116/tcp+udp, 21118/tcp; hbbr 21117/tcp, 21119/tcp. Firewall: INPUT ACCEPT (открыто).
- Установка сервера воспроизводится скриптом `server_install.sh` (этой папки), запуск на VPS: `ssh root@VPS 'bash -s' < server_install.sh`.
- 🔴 Перед установкой на этом VPS был удалён кривой `rudesktop` (RuDesktop, `--dispatcher` на 21116/udp) — по указанию владельца.

## 4. Настройки обеих сторон (эталон)

🔴 **Этот раздел — ЗЕРКАЛО.** Он одинаков в двух файлах, лежащих в РАЗНЫХ репозиториях:
`/work/kso/docs/INSTRUCTION_rustdesk_kassa.md` (репо kso) и `/work/vnc_work_v2-vm121-review/rustdesk/README.md`
(репо vnc_work). Меняешь настройки — правь ОБА файла и коммить оба репозитория, иначе следующая
сессия будет чинить канал по устаревшему эталону.

RustDesk соединяет две стороны, только если ОБЕ зарегистрированы на ОДНОМ сервере. Поэтому
значения из 4.0 обязаны совпадать у всех наших машин; всё остальное — местные детали.

### 4.0 Общее для всех машин

| Параметр | Значение |
|---|---|
| ID Server / «Сервер ID» | `178.253.55.128` (наш VPS, Aeza, RU) |
| Key (публичный ключ hbbs) | `OS6FFMq66QzmnraQDy+cy+fdryWDXA0fBVZTbYqc7Lk=` |
| Relay Server / «Ретранслятор» | можно НЕ задавать — hbbs отдаёт релей сам |
| API Server | пусто. 21114 у нас НЕТ по дизайну |
| Порты | hbbs `21115/tcp`, `21116/tcp+udp`, `21118/tcp`; hbbr `21117/tcp`, `21119/tcp` |
| Службы на VPS | `rustdesk-hbbs.service`, `rustdesk-hbbr.service` (systemd, enabled, Restart=always) |
| Данные и ключ сервера | `/var/lib/rustdesk-server/`, бинарники `/usr/local/bin/{hbbs,hbbr}` |
| Доступ к VPS | `root@178.253.55.128:22`, пароль — `/home/hgff/vps for cron.txt` |

Установка сервера воспроизводится: `ssh root@VPS 'bash -s' < /work/vnc_work_v2-vm121-review/rustdesk/server_install.sh`.

### 4.1 Наша сторона — управляющий клиент в WSL (дисплей `:99`, пользователь `hgff`)

Конфиг: **`/home/hgff/.config/rustdesk/RustDesk2.toml`**

```toml
rendezvous_server = '178.253.55.128:21116'
nat_type = 0
serial = 0

[options]
custom-rendezvous-server = '178.253.55.128'
relay-server = '178.253.55.128'
key = 'OS6FFMq66QzmnraQDy+cy+fdryWDXA0fBVZTbYqc7Lk='
```

- `av1-test`, `local-ip-addr`, `nat_type`, `serial` — служебные, RustDesk пишет их сам, не трогать.
- Рядом лежит **`RustDesk.toml` — это НАШ ID, пароль и ключевая пара.** Не публиковать и не копировать
  на другие машины: два клиента с одним ID начнут перебивать регистрацию друг друга.
- Правится при ОСТАНОВЛЕННОМ клиенте, затем перезапуск (см. ниже), иначе файл перезапишется обратно.
- Стек `:99` (Xvfb + fluxbox + x11vnc на 5902) поднимает **root**; человек смотрит тот же экран
  своим TigerVNC на `localhost:5902`.
- Поднять/починить канал: `bash /work/vnc_work_v2-vm121-review/rustdesk/rd_health.sh up` (идемпотентна),
  проверить — `check` (exit 0/1), посмотреть процессы и порты — `status`. Всё с `dangerouslyDisableSandbox`.
- 🔴 Клиент «протухает»: провисев больше суток, он уходит в петлю heartbeat/NAT, и тогда
  `rustdesk --connect <ID>` молча выходит с кодом 0 и НЕ открывает окно (диагноз T110). Лечится
  перезапуском нашей стороны; профилактика — cron `17 5 * * *` → `rd_health.sh refresh`
  (пропускает себя, если идёт живая сессия).

### 4.2 Касса КСО — RustDesk ID `243540605`, `DESKTOP-E8FF2LU`

Установлен не портативный: `C:\Program Files\RustDesk`, служба `rustdesk` (Automatic, LocalSystem).

🔴 **Конфигов ДВА комплекта, боевой — служебный:**

| Кто читает | Путь |
|---|---|
| **Служба + `--server`** (обслуживает входящие) — **боевой** | `C:\Windows\ServiceProfiles\LocalService\AppData\Roaming\RustDesk\config\` |
| GUI/трей пользователя `SCO_M210` (косметика) | `C:\Users\SCO_M210\AppData\Roaming\RustDesk\config\` |
| `C:\ProgramData\RustDesk` | **не существует** — не искать там |

Правильное содержимое служебного `RustDesk2.toml`:

```toml
rendezvous_server = '178.253.55.128:21116'
[options]
custom-rendezvous-server = '178.253.55.128'
key = 'OS6FFMq66QzmnraQDy+cy+fdryWDXA0fBVZTbYqc7Lk='
```

- ⚠️ `relay-server` на кассе НЕ задан — это норма (hbbs отдаёт релей сам). У нас он задан явно;
  оба варианта рабочие, приводить к одному виду не требуется.
- ⚠️ В **пользовательском** `RustDesk2.toml` поле `rendezvous_server` = легаси
  `rs-ny.rustdesk.com:21116`. **Это не поломка:** `custom-rendezvous-server` в `[options]` имеет
  приоритет. Не «чинить».
- Пароль: постоянный (unattended), режим «по паролю И по кнопке» → мы заходим с `:99` **без
  подтверждения на экране кассы**. Сам пароль — `/work/kso/chat/rustdesk_kassa.md` (в git не попадает).
- Восстановление после сбоя службы задано (T110): `sc failure rustdesk reset= 86400
  actions= restart/60000/restart/60000/restart/120000` + `sc failureflag rustdesk 1`.
- Логи службы: `…\LocalService\AppData\Roaming\RustDesk\log\server\` (файл на сутки, ~3.5 МБ/сут).
  `RustDesk_rCURRENT.log` — симлинк на текущий, читать датированный файл.

**Четыре процесса `rustdesk.exe` — это НОРМА, а не дубли:**

```
RustDesk.exe --service   SessionId 0   служба
RustDesk.exe --server    SessionId 1   обслуживает входящие соединения
rustdesk.exe --tray      SessionId 1   иконка в трее (живёт с логона, может быть сильно старше остальных)
RustDesk.exe --cm        SessionId 1   «connection manager» — появляется НА ВРЕМЯ нашей сессии
```

🔴 В T110 старый `--tray` (на 5 суток старше службы) приняли за «залипший дубль». Это не дубль —
у каждой роли ровно один процесс, а трей лишь рисует иконку. Настоящий дубль — два процесса с
ОДИНАКОВЫМ ключом (`--server` + `--server`).
🔴 Снимать на кассе можно только `rustdesk.exe`. Рядом работают киоск (`electron.exe`) и 1С-агент
(`1cv8`) — касса торгует, их не трогать.

### 4.3 Остальные машины на нашем сервере

**КСО №2 — RustDesk ID `288860502`, `desktop-89hr2jf`** (заведена 29.07.2026).
Установлен НЕ портативный: `C:\Program Files\RustDesk`, служба `rustdesk` Running, `sc failure`
настроен как на первой кассе. Наш сервер прописан в ОБА конфига (служебный — боевой). Постоянный
пароль задан, вход без подтверждения проверен живьём (в заголовке сессии появился hostname).
Пароль — в `/work/kso/CREDENTIALS.md`. Бутстрап шёл через AnyDesk `1658665255`.
🔴 Грабли онбординга (подробно — `/work/vnc_work_v2-vm121-review/FIELD_NOTES.md` §9): через AnyDesk теряются заглавные
буквы при наборе, `Ctrl+V` в консоли Windows не вставляет, а в окна «от администратора» ввод не
проходит вовсе (UIPI). Рабочий приём: длинную команду — в буфер, руками набрать строчными
`iex (get-clipboard)`; админские шаги — через `start-process powershell -verb runas -encodedcommand`.

| Машина | RustDesk ID | Особенности |
|---|---|---|
| Сервер 1С `DESKTOP-VGVHEOU` | `239677631` | постоянный пароль (в `CREDENTIALS.md`), SSH-туннель 2244 |
| ЭЦП/ОФД `DESKTOP-I7OE05B` (AnyDesk `121019169`) | `474932946` | **portable, автозапуска нет** — рунбук `/work/kso/docs/INSTRUCTION_rustdesk_121019169.md`; SSH-туннель 2245 |
| Машина клиента ZennoLab | `2512413` | режим «принимать нажатием кнопки», пароля нет — заходим только с подтверждением |

🔴 Чужие машины не переключать на другой сервер и не менять им пароль: у них оборвётся канал,
а у ЭЦП/ОФД машина вообще не наша (только чтение и лёгкие пробы).

### 4.4 Как завести НОВУЮ машину на нашем сервере

📄 **Пошаговый боевой протокол с граблями — `ONBOARD_via_anydesk.md`** (рядом с этим файлом;
копия в проекте: `/work/kso/docs/INSTRUCTION_rustdesk_via_anydesk.md`). Там разобран прогон
29.07.2026 (КСО №2): три ограничения канала AnyDesk (теряются заглавные буквы, `Ctrl+V` в консоли
не вставляет, в админские окна ввод не проходит из-за UIPI), приём «команда в буфер + строчными
`iex (get-clipboard)`», запуск админских шагов через `runas -encodedcommand` и критерий приёмки
(hostname в заголовке сессии).

1. На машине: меню **три точки (⋮) СЛЕВА от ID** в секции «Ваш рабочий стол» → **Настройки** →
   раздел **Сеть** → строка **ID/Ретранслятор**. (Ссылка «настройте собственный сервер» внизу окна
   ведёт на веб-страницу, а не в настройки — не она.)
2. Сервер ID = `178.253.55.128`, Key = ключ из 4.0, Ретранслятор и API — пусто. OK.
   Признак, что применилось: на вкладке «Сеть» появляются тумблеры «Разрешать небезопасные TLS»
   и «Отключить UDP».
3. Пароль: **«Установить постоянный пароль»** — для касс и серверов, чтобы заходить без
   подтверждения. Если авто-вход нежелателен — «Принимать сеансы нажатием кнопки» (тогда поля
   пароля исчезают и по паролю зайти нельзя).
4. Проверка: в логе самой машины появляются строки `Latency of 178.253.55.128:21116` (см. раздел
   про регистрацию на hbbs). Бутстрап-канал на время настройки — AnyDesk,
   рунбук `/work/kso/docs/INSTRUCTION_anydesk_vnc_wsl.md`.
5. Записать ID, пароль и порт туннеля в `CREDENTIALS.md`, машину — в этот раздел 4.3 (в ОБА файла).

---

## Как подключиться к кассе (повседневно)
Одной командой (поднимает RustDesk на :99 и коннектит к кассе через наш сервер):
```
bash /work/vnc_work_v2-vm121-review/rustdesk/connect.sh
```
Затем смотреть/управлять примитивами `x99.sh`:
```
bash /work/vnc_work_v2-vm121-review/rustdesk/x99.sh shot                 # скрин :99 → /tmp/x99.jpg
bash /work/vnc_work_v2-vm121-review/rustdesk/x99.sh zoom 700x420+560+230 250%
bash /work/vnc_work_v2-vm121-review/rustdesk/x99.sh click X Y
bash /work/vnc_work_v2-vm121-review/rustdesk/x99.sh type 'текст'         # латиница/цифры/спецсимволы (локально на :99, раскладка US)
bash /work/vnc_work_v2-vm121-review/rustdesk/x99.sh key ctrl+v
bash /work/vnc_work_v2-vm121-review/rustdesk/x99.sh wins                 # список окон
```
Человек смотрит тот же :99 по VNC: `localhost:5902`.

## 🔴 Важные грабли
- **Песочница режет долгие процессы сигналом 16 (exit 144).** Все команды канала (rustdesk/anydesk/Xvfb на :99,
  ssh к VPS долгий) запускать с `dangerouslyDisableSandbox`.
- **Self-match pkill:** не писать в командах строки `Xvfb :99` / `rustdesk_kso.sh up` рядом с pkill — скрипты
  внутри делают `pkill -f "Xvfb :99"` и убивают сам шелл. Глушить сирот по явному PID.
- **Сироты `up`:** старый драйвер `rustdesk_kso.sh up` зависает и не выходит → копятся дубли, рвущие коннект
  (`kill_on_display rustdesk` по кругу). Здесь используем GUI-подход (connect.sh), без голого `--connect`.
- **Раскладка кассы:** ввод латиницы/спецсимволов прямо НА кассе (через канал) может уехать в кириллицу —
  проверять зумом. Ввод в НАШИ локальные диалоги (:99) безопасен (раскладка US).

## Проверка регистрации на сервере
🔴 `journalctl -u rustdesk-hbbs | grep update_pk` **НЕ РАБОТАЕТ** (проверено T110, 28.07.2026):
`update_pk` пишется на уровне `debug!`, а hbbs крутится на `info` — в журнале пусто ВСЕГДА,
независимо от того, зарегистрирована машина или нет. Рабочие способы (лог самой машины,
`tcpdump` на VPS, база пиров) — в рунбуке `/work/kso/docs/INSTRUCTION_rustdesk_kassa.md` §3.

## Клавиша Super/Win НЕ пробрасывается (и это НЕ нужно чинить)
Симптом: `xdotool key super+r` на :99 НЕ открывает «Выполнить» на кассе — Win-комбо не доходят.
Причина: локальный WM (fluxbox) и X-сервер на :99 перехватывают Super раньше, чем RustDesk его форвардит
(плюс RustDesk на Linux→Windows непредсказуемо шлёт именно Win-клавишу). Это НЕ влияет на работу.

**Можно без Super — всё остальное форвардится нормально:**
- Мышь (Пуск, иконки рабочего стола, таскбар, поиск) — работает; клик СНАЧАЛА внутрь удалённого экрана
  (дать фокус сессии), потом действия.
- Обычные клавиши/комбо: Ctrl+C / Ctrl+V / Ctrl+A, Alt+F4, Tab, Enter, стрелки, печать текста — доходят.
- «Выполнить» открывать НЕ через Win+R, а: клик по Пуску → набрать команду, либо двойной клик по ярлыку,
  либо поиск в таскбаре.
- Ctrl+Alt+Del / спец-клавиши при нужде — через тулбар самой RustDesk-сессии (иконки сверху окна).

Чинить (если когда-то реально понадобится Win): в RustDesk переключить режим клавиатуры (Map/Legacy) в тулбаре
сессии, либо убрать перехват Super у fluxbox. На практике не требуется — обходится мышью/Пуском.

---

## Онбординг ещё одной машины на наш сервер (пример: клиент ZennoLab, 2026-06-29)

Подключили вторую машину к нашему RustDesk-серверу, **не трогая канал кассы** (243540605
остаётся на нашем сервере; наш :99-клиент с сервера НЕ переключаем — иначе касса отвалится).

**Машина клиента:** hostname `win-9s37b63umah`, RustDesk ID `2512413`. RustDesk у неё в
ПОРТАТИВНОМ режиме (панель «Установить…», только одноразовый пароль) — изначально сидела на
чужом (публичном/дефолтном) сервере, поэтому наш сервер её не видел: коннект давал
**`Connection error — ID does not exist`**.

**Бутстрап-канал — AnyDesk** (на машине клиента уже установлен): AnyDesk ID `1413360810`,
без пароля → подключение с подтверждением «Accept» на стороне клиента. ⚠️ Free-AnyDesk рвётся
каждые ~5–8 мин («Connection Closed»), поэтому через него только разовая настройка, не работа.

### Шаги настройки (на машине клиента, через AnyDesk)
1. **Перенаправить RustDesk клиента на наш сервер.**
   🔴 **Как открыть Настройки RustDesk (проверено 2026-07-06):** кнопка меню — это **три точки (⋮) СЛЕВА от ID
   устройства**, в секции «Ваш рабочий стол» (рядом с ID и одноразовым паролем). Клик → в меню выбрать
   «Настройки». ❌ НЕ ссылка «Для более быстрого подключения настройте собственный сервер» внизу окна — она
   ведёт на ВЕБ-страницу, не в настройки. ❌ Справа/в заголовке окна меню настроек НЕТ.
   В окне «Настройки»: слева раздел **«Сеть»** → строка **«ID/Ретранслятор»** (не SOCKS, не websocket):
   - Сервер ID = `178.253.55.128`
   - Key = `OS6FFMq66QzmnraQDy+cy+fdryWDXA0fBVZTbYqc7Lk=`
   - Ретранслятор/Сервер API — пусто. OK.
   - Признак, что сервер применился: на вкладке «Сеть» появляются доп-тумблеры
     «Разрешать небезопасные TLS», «Отключить UDP».
2. **Пароль — два режима, выбирай по задаче** («Настройки → Безопасность → Пароль», выпадающий список):
   - 🟢 **АВТО-ВХОД по постоянному паролю (наш случай для касс — чтобы подключаться без подтверждения):**
     выбрать **«Установить постоянный пароль» / «Use permanent password»** → ввести пароль дважды → OK.
     Тогда с нашего `:99` заходим по этому паролю без ручного «Принять» на той стороне.
     ✅ Проверено на кассе `712070951` / RustDesk ID `239677631` (2026-07-06). Открытие Настроек — три точки ⋮
     СЛЕВА от ID (см. шаг 1). Пароль храним в `/work/kso/CREDENTIALS.md`.
   - 🔒 **Только ручное подтверждение (если авто-вход нежелателен, кейс ZennoLab):** выбрать **«Принимать сеансы
     нажатием кнопки»** (approve-mode = click only). После этого поля пароля исчезают — каждое входящее
     подключение требует ручного «Принять» на той стороне, по паролю зайти нельзя.

### Проверка регистрации на сервере
```
ssh root@178.253.55.128 'journalctl -u rustdesk-hbbs --no-pager -n 120 | grep update_pk'
# должны быть и 2512413 (клиент), и 243540605 (касса — не задели)
```
Доступ к VPS: `/home/hgff/vps for cron.txt` (root@178.253.55.128, пароль в файле).

### Подключение к клиенту (повседневно)
С нашего :99-клиента: ввести `2512413` → «Подключиться» (или `rustdesk --connect 2512413`).
На рабочем столе клиента всплывёт «Принять?» — принимает человек у клиента, либо мы через
AnyDesk. Сессия установлена, когда в заголовке окна появляется `2512413@win-9s37b63umah`
(RustDesk показывает hostname только после успешной аутентификации).

### Грабли этого онбординга
- **`rustdesk --connect <ID>` молча выходит (exit 0) и не открывает окно**, если уже запущенный
  экземпляр RustDesk завис в несвежем состоянии (петля heartbeat/NAT). Лечится перезапуском
  нашего :99-клиента: `pkill -x rustdesk` (именно `-x`, чтобы не убить runuser-обёртки и свой
  bash), затем заново поднять `rustdesk --service` и `rustdesk` (фоновые задачи + dangerouslyDisableSandbox).
  После рестарта Connect/`--connect` сразу открывает окно сессии.
- В логе `~/.local/share/logs/RustDesk/rustdesk_rCURRENT.log` постоянно: `test nat: Failed to
  connect to …:21115` и `HTTP POST …:21114/api/heartbeat failed (IncompleteMessage)`. Это **НЕ
  фатально**: 21114 (API) у нас нет по дизайну, а NAT-проба к 21115 не мешает коннекту через
  relay 21117 (регистрация по 21116/UDP проходит — `update_pk` в логах сервера это подтверждает).
- Клик «Принять» через само окно RustDesk-сессии невозможно (ввод не форвардится до
  подтверждения) — принимаем либо у клиента, либо через параллельный AnyDesk.

---

## Канал лёг: одна команда (T110, 28.07.2026)

```
bash /work/vnc_work_v2-vm121-review/rustdesk/rd_health.sh up      # поднять/починить канал к кассе
bash /work/vnc_work_v2-vm121-review/rustdesk/rd_health.sh check   # жив ли (exit 0/1, печатает заголовок сессии)
bash /work/vnc_work_v2-vm121-review/rustdesk/rd_health.sh status  # процессы + дисплей :99 + порты hbbs
```
📄 **Полный рунбук: `/work/kso/docs/INSTRUCTION_rustdesk_kassa.md`** — как отличить «касса офлайн»
от «лёг только канал», где лежат конфиги (их ДВА комплекта), в каком порядке что перезапускать.

🔴 **«Connection error / Remote desktop is offline» ВРЁТ.** Это «мой клиент не смог договориться»,
а не «машина мертва». В T110 касса всё время торговала и непрерывно регистрировалась на нашем hbbs,
а виноват был наш клиент на `:99`, провисевший ~1 д 20 ч (та самая гоча «`--connect` молча выходит
с кодом 0»). Сначала всегда чинить свою сторону — `rd_health.sh up`.

🔴 **Про «Проверку регистрации на сервере» выше: `grep update_pk` не работает.** `update_pk` в
rustdesk-server пишется на уровне `debug!`, а hbbs крутится на дефолтном `info` → в журнале пусто
(за месяц 3 строки «new version is available»). Реальная проверка — по логу САМОЙ машины:
строки `Latency of 178.253.55.128:21116` в
`C:\Windows\ServiceProfiles\LocalService\AppData\Roaming\RustDesk\log\server\*.log`.
Подробности и готовые команды — в рунбуке.

🔴 **Четыре `rustdesk.exe` на кассе — это норма, а не дубли:** `--service` (сессия 0), `--server`
(входящие), `--tray` (иконка, живёт с логона и бывает сильно старше остальных), `--cm` (появляется
на время нашей сессии). У каждой роли ровно один процесс.

**Устойчивость:** cron root `17 5` — `rd_health.sh refresh` (перезапуск нашего демона, сам себя
пропускает при живой сессии); `27 5` — `rd_health.sh prune 7 500` (логи RustDesk у нас росли на
0.5–1 ГБ/сут и набрали 2.7 ГБ). На кассе службе `rustdesk` задан restart-on-failure (`sc failure`,
60/60/120 с) — раньше действий не было.

---
## См. также: запуск на удалённой Windows БЕЗ GUI (когда RustDesk-вождение не годится)
Если `schtasks /s` и WMI/DCOM заблокированы файрволом/антивирусом, но SMB(445) жив — команды на машине
запускаются через `sc.exe \\<ip>` (svcctl по SMB) транзитной службой, без GUI. Рецепт и reverse-SSH
onboarding: `docs/REMOTE_EXEC_no_gui_sc_over_smb.md` (применено на SOCHI11, туннель VPS:2249, T228).
